[project]
name = "cosmotgg"
version = "0.0.0"
requires-python = ">=3.13"

[project.optional-dependencies]
notebook = [
    "nbformat==5.10.4",
    "nbclient==0.11.0",
    "ipykernel==7.3.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
