"""`cosmotgg.models.model0d` — toy0d model-specific assembly.

`model0d` tests whether two projected relative modular generators on a
common finite-dimensional type-I overlap algebra `B` (`chi_source`,
`chi_target`, supplied upstream e.g. by `cosmotgg.models.model0c`, but
never imported here in production, `MODEL0D_PRODUCTION_IMPORTS_MODEL0C
= NO`) admit a finite, directed, invertible contextual-state transport,
without selecting any real modular flow parameter
(`cosmotgg.models.model0d.transport`).

Normative sources:

- `docs/toy-models/toy0d/specification.md`
- `docs/toy-models/toy0d/implementation-design.md`
"""
