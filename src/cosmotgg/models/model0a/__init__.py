"""`cosmotgg.models.model0a` — toy0a model-specific assembly.

`model0a` is the first candidate construction of the `toy0a` toy model:
a finite Connes cocycle between the two-qubit, fixed-marginal correlation
state family (`cosmotgg.models.model0a.states`) and the corresponding
product-of-marginals reference state.

Normative sources:

- `docs/toy-models/toy0a/specification.md`
- `docs/toy-models/toy0a/implementation-design.md`
"""
