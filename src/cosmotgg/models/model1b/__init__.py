"""`cosmotgg.models.model1b` — toy1b bounded T5-FLOW qualification mechanism.

`model1b` tests whether a deterministic state coarse-graining by partial
trace, followed by the full modular datum `K_n = -log(rho_n)`, supports a
coherent, frame-covariant, flatness-preserving, and nontrivially running
finite cross-scale relational structure, on the fixed eight-site fine cycle
`Gamma_2 = A <- X <- Y <- B <- C <- P <- Q <- D <- A` decimated
`8 -> 6 -> 4` (`cosmotgg.models.model1b.hierarchy`,
`cosmotgg.models.model1b.states`, `cosmotgg.models.model1b.modular_support`,
`cosmotgg.models.model1b.directional`).

`model1b` does NOT attempt `T5 PASS`, continuum reconstruction, metric
emergence, Riemann curvature, physical tidal gravity, `T4`, `T6`, `T7`,
dark matter, cosmology, or `C_rel`.

`MODEL1B_PRODUCTION_IMPORTS_PRIOR_MODELS = NO`: this package imports no
`cosmotgg.models.model0a`-`model0e`/`model1a` production API.

Normative sources:

- `docs/toy-models/toy1b/specification.md`
- `docs/toy-models/toy1b/implementation-design.md`
- `docs/model/t5-modular-cross-scale-flow-criteria.md`
"""
